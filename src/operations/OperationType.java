package operations;


public enum OperationType {
    ADD,
    SUBTRACT,
    MULTIPLY,
    MOVE_IMMEDIATE,
    JUMP_IF_EQUAL,
    AND,
    XOR_IMMEDIATE,
    JUMP,
    LOGICAL_SHIFT_LEFT,
    LOGICAL_SHIFT_RIGHT,
    MOVE_TO_REGISTER,
    MOVE_TO_MEMORY
}
